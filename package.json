{
  "name": "the-human-channel-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "docusaurus start",
    "build": "docusaurus build",
    "deploy": "GIT_USER=the-human-channel docusaurus deploy",
    "predeploy": "docusaurus build",
    "serve": "serve -s build"
  },
  "dependencies": {
    "@docusaurus/core": "^3.8.0",
    "@docusaurus/preset-classic": "^3.8.0"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
